spring:
  application:
    name: demo

  datasource:
    url: jdbc:postgresql://localhost:5432/health_db
    username: postgres
    password: adeilton000
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update

  flyway:
    enabled: true
